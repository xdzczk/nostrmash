FROM golang:1.26.2

RUN apt-get update \
	&& apt-get install -y --no-install-recommends bash make git build-essential ca-certificates \
	&& rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

CMD ["make", "ci"]
